# Loaded for production builds (npm run build / CI). No secrets—public API origin only.
# Change to your backend when deploying elsewhere; no trailing slash.
VITE_API_URL=https://ai-tutor-3roc.onrender.com
