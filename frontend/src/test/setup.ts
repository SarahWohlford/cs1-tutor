// Vitest global setup. jest-dom matchers (toBeInTheDocument, etc.) are registered
// here so the jsdom component specs can use them. Pure-logic specs ignore them.
import "@testing-library/jest-dom/vitest";
